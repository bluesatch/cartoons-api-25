<%- include('../partials/header') %>

    <main class="main" id="homeMain">
        <div class="container">
            <figure class="figure home-figure d-block mx-auto">
                <img src="<%= cartoon.image %>" alt="<%= cartoon.title %> " class="figure-img img-fluid home-img d-block mx-auto">
                <figcaption class="figure-caption text-center">
                    <a href="/cartoons2d/<%= cartoon.id %>" class="cartoon-link">
                        <%= cartoon.title %>
                    </a>
                </figcaption>
            </figure>
        </div>
    </main>

<%- include('../partials/footer') %>

